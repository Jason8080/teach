<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 指定操作类型(指定文件归属的操作类) -->
<mapper namespace="com.itheima.mybatis.day04.cache.mapper.OrderMapper">

    <resultMap id="OrderMap" type="Order">
        <result column="user_id" property="userId"/>
        <association property="user" javaType="user"
                     select="com.itheima.mybatis.day04.cache.mapper.UserMapper.findById"
                     column="user_id"
        />
    </resultMap>


    <!-- 指定操作名称; 指定封装类; 执行语句 -->
    <select id="findO2O" resultMap="OrderMap">
        select * from orders
    </select>

    <!-- 指定操作名称; 指定封装类; 执行语句 -->
    <select id="findByUserId" parameterType="int" resultType="order">
        select * from orders where user_id = #{id}
    </select>


</mapper>